{
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "installCommand": "npm ci --include=dev",
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
